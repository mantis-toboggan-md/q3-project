front:

  react-router-dom to separate index into pages:
    items X
    cart/checkout X
    admin login X
    admin update page X

  admin update page X
  add new item admin page
  delete item on admin page X
  admin route protection X
  auth, admin login X
  display subtotal in cart, total in checkout X

  checkout page should only submit if cart doesn't exceed inventory
  improve info display on plants X
  style index X
  style checkout
  add navbar X
  cart dropdown on index X
  filter drawer on index X
  filter by name X


back:

  checkout route to update inventory
  delete item route X
